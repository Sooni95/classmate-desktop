# ClassMate 데스크탑 스토어 출시 — 전체 체크리스트

대상: **Microsoft Store(1순위)** + **Mac 서명·공증 DMG 자체배포(권장)** / Mac App Store(선택·고난도)

---

## A. 공통 사전 준비
- [ ] **고해상도 아이콘 원본 확보**: 현재 `build/icon.png`는 128px 로고를 1024로 업스케일한 임시본입니다. 디자이너에게 **1024×1024 원본 PNG(투명 배경)**를 받아 `build/icon.png`로 교체하세요. (스토어 아이콘 선명도 직결)
- [ ] **버전 규칙 정하기**: `package.json`의 `version`을 출시마다 올림(예: 1.0.0 → 1.0.1). MSIX는 4자리(1.0.0.0) 권장.
- [ ] **개인정보처리방침 URL**: 두 스토어 모두 필수. AI 보조(Anthropic), 음성 자막(STT), 의견 보내기에서 데이터가 어떻게 처리되는지 1페이지로. (코코아팹 도메인에 호스팅)
- [ ] **지원 연락처**: ksh0502@nepes.co.kr 또는 코코아팹 대표 메일.

---

## B. Microsoft Store (Windows) — 1순위

### B-1. 계정·식별자
- [ ] **Microsoft 파트너센터** 가입: partner.microsoft.com → 개발자 등록(회사 계정 권장).
- [ ] 파트너센터에서 **앱 이름 예약**("ClassMate" 또는 "ClassMate 수업도구" 등 — 중복 시 변형).
- [ ] 예약하면 **Identity 값**이 발급됩니다. `package.json`의 `build.appx`를 실제 값으로 교체:
  - `identityName` → 파트너센터 "앱 ID" 페이지의 **Package/Identity/Name** (예: `12345NEPESCocoafab.ClassMate`)
  - `publisher` → **Package/Identity/Publisher** (예: `CN=ABCD1234-...`)
  - `publisherDisplayName` → **Package/Properties/PublisherDisplayName** (예: `NEPES Cocoafab`)

> 위 세 값이 정확히 일치하지 않으면 업로드가 거부됩니다. 빌드 자체는 플레이스홀더로도 되지만, **제출 전 반드시 교체**.

### B-2. 빌드 (이미 설정됨)
- 코드 변경 없이 `npm run dist` 가 **NSIS 설치본 + 포터블 + .appx** 를 함께 생성합니다.
- GitHub Actions 빌드 후 **Actions → 해당 실행 → Artifacts → `ClassMate-MSIX-store`** 에서 `.appx` 다운로드.
- **서명 불필요**: Store에 올리면 Microsoft가 서명합니다(사이드로드 테스트 시에만 임시 인증서 필요).

### B-3. 스토어 등록 정보 (아래 "리스팅 문안" 사용)
- [ ] 카테고리: **교육(Education)**
- [ ] 스크린샷: 1366×768 이상 4~8장 (오버레이/타이머/뽑기/칠판/발표자/Pro 등)
- [ ] 권한 사유(심사용) — 아래 "권한 설명" 그대로 활용
- [ ] 연령 등급 설문(전체 이용가)

### B-4. 심사 유의 (오버레이 앱 특성)
- ClassMate는 "항상 위 + 투명 오버레이 + (Pro)화면 캡처/마이크"라 심사관이 동작을 물을 수 있음 → **리스팅에 용도를 명확히** 기재.
- 자동 업데이트/백그라운드 상주 없음, 광고 없음 — 이 점을 명시하면 통과 수월.

---

## C. macOS — 서명·공증 DMG 자체배포 (권장)

> Mac App Store는 **샌드박스 강제**라 오버레이·화면캡처·항상위 기능이 리젝될 위험이 큽니다.
> 따라서 **MAS 대신, 공증된 DMG를 코코아팹/깃허브에서 배포**하는 것을 권장합니다(설치 경고 없이 열림).

- [ ] **Apple Developer Program** 가입(연 $99).
- [ ] **Developer ID Application** 인증서 발급.
- [ ] electron-builder 서명·공증 설정(아래 환경변수) 후 `npm run dist:mac`:
  - `CSC_LINK`(인증서 .p12 base64), `CSC_KEY_PASSWORD`
  - `APPLE_ID`, `APPLE_APP_SPECIFIC_PASSWORD`, `APPLE_TEAM_ID` → electron-builder가 자동 notarize
  - `package.json`의 `mac.hardenedRuntime`를 `true`로, `identity`는 제거(또는 인증서 이름) — 공증엔 hardenedRuntime 필요
- [ ] 권한 사유 plist(entitlements): 화면 기록·마이크 사용 설명. (Pro 기능)
- [ ] 최초 실행 안내: 화면 기록 권한 허용(시스템 설정 → 개인정보 보호 → 화면 기록).

### (선택) Mac App Store까지 가려면
- `mas` 타깃 + 샌드박스 entitlements + 화면캡처 대체 API 필요 → 별도 개발. 1차 출시엔 비권장.

---

## D. 출시 후
- [ ] 자동 업데이트: 현재 없음. 추후 `electron-updater` + GitHub Releases로 추가 가능(자체배포 한정; Store판은 스토어가 업데이트 관리).
- [ ] 버전 올릴 때 `version` 변경 → 태그 push → CI 빌드 → 스토어 재제출.

---

# 리스팅 문안 (복사용)

**이름**: ClassMate — 수업 보조 도구
**짧은 설명(한 줄)**: 화면 위 하나의 툴바로 끝내는 교사용 올인원 수업 도구.

**설명(본문)**:
ClassMate는 타이머·발표자 뽑기·QR·화면 필기·메모·포인터·소음 측정·사다리·핀볼·칠판·발표자 모드까지, 수업에 필요한 도구를 화면 위 하나의 툴바에 모은 교사용 올인원 보조 프로그램입니다. PPT·교과서·웹 등 어떤 화면 위에든 떠 있어, 도구를 바꿀 때마다 창을 옮기느라 수업 흐름이 끊기던 문제를 없앱니다.

- 화면 위 투명 오버레이 + 클릭 통과로 수업 흐름 유지
- 타이머·발표자 뽑기·사다리·핀볼·점수판 등 수업 활동
- 펜·형광·도형·스탬프·텍스트, 돋보기·스포트라이트 포인터
- 칠판(초록칠판/화이트보드) — 창 크기 조절·텍스트 입력
- 발표자 모드 — 대본 텔레프롬프터·타이머·카운트다운·레이저
- (Pro) 명단 저장, AI 수업 보조, 실시간 음성 자막, 다문화 번역
- 광고 없음 · 백그라운드 상주 없음

**카테고리**: 교육
**키워드**: 수업도구, 교사, 타이머, 발표자뽑기, 전자칠판, 수업보조, 오버레이

---

# 권한 설명 (심사·등록용)

- **항상 위/투명 오버레이**: 수업 자료(PPT 등) 위에 도구 툴바를 띄우기 위함. 다른 앱의 입력을 가리지 않도록 평소 클릭 통과 상태입니다.
- **마이크**(소음 측정기, Pro 음성 자막): 교실 소음 크기를 시각화하거나, 발화를 실시간 자막/번역으로 보조하기 위함. 녹음 저장은 하지 않습니다.
- **화면 캡처**(돋보기/스포트라이트, 일부 기록): 화면 일부를 확대해 학생에게 보여주기 위함.
- **네트워크**(Pro·의견 보내기): AI 보조·번역·자막 처리 및 사용자 의견 전송. 핵심 도구는 오프라인 동작.
- 개인정보는 광고·추적 목적으로 수집/판매하지 않습니다.
